// Trivial smoke script for `firebase emulators:exec`.
// Exits 0 after printing a sentinel so the exec wrapper confirms the
// firestore + auth emulators started cleanly. See Task 1 of the online-play plan.
console.log('emulators up');
