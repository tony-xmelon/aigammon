name: lan_play
description: >-
  Pure-Dart wire protocol, bidirectional relay and MatchTransport for AIGammon
  nearby (LAN) play.
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  backgammon_core:
    path: ../backgammon_core
  match_transport:
    path: ../match_transport

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
